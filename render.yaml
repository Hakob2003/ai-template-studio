databases:
  - name: ai-studio-db
    plan: free
    databaseName: aistudio
    user: studio

services:
  # Backend API
  - type: web
    name: ai-studio-backend
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: npm ci && npx prisma generate && npm run build
    startCommand: npx prisma migrate deploy && npx prisma db seed && node dist/server.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
      - key: DATABASE_URL
        fromDatabase:
          name: ai-studio-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: REFRESH_SECRET
        generateValue: true
      - key: ENCRYPTION_KEY
        generateValue: true
      - key: STORAGE_MODE
        value: local
      - key: FRONTEND_URL
        value: https://ai-studio-frontend.onrender.com
      - key: BACKEND_URL
        value: https://ai-studio-backend.onrender.com

  # Redis
  - type: redis
    name: ai-studio-redis
    plan: free
    maxmemoryPolicy: allkeys-lru
    ipAllowList: []

  # Frontend
  - type: web
    name: ai-studio-frontend
    runtime: node
    plan: free
    rootDir: frontend
    buildCommand: npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_API_URL
        value: https://ai-studio-backend.onrender.com/api
      - key: NEXT_PUBLIC_WS_URL
        value: https://ai-studio-backend.onrender.com
