const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function main() { const user = await prisma.user.findUnique({ where: { email: "user@aistudio.com" } }); if (!user) { console.error("User not found"); return; } await prisma.aIConnection.upsert({ where: { userId_provider: { userId: user.id, provider: "STABLE_DIFFUSION" } }, update: { isActive: true, baseUrl: "http://host.docker.internal:7860" }, create: { userId: user.id, provider: "STABLE_DIFFUSION", modelId: "sdxl", baseUrl: "http://host.docker.internal:7860", isActive: true }, }); console.log("Stable Diffusion connector updated to host.docker.internal"); } main().catch(console.error).finally(() => prisma.$disconnect());
